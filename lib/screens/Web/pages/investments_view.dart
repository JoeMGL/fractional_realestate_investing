import 'package:flutter/material.dart';

class InvestmentsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Investments View Coming Soon',
        style: TextStyle(color: Colors.white, fontSize: 18),
      ),
    );
  }
}
